# 2026-06-27 — SMA4 1-2 Terminal Clear and Card-Return Finisher

## Result

SMA4 1-2 now terminal-clears from the cached overworld entry snapshot:

- Entry snapshot: `runs/sma4_cache/1-2_entry.pkl`
- Source P-speed prefix: `runs/20260626-201118-sma4_snapshot_1_2_pspeed/attempt_summary.json`
- Replay-verified solution: `data/solutions/sma4/1-2.json`
- Run summary: `runs/20260627-104611-sma4_snapshot_1_2_pspeed_finish/attempt_summary.json`
- Contact sheet: `runs/20260627-104611-sma4_snapshot_1_2_pspeed_finish/solved_contact.png`

Final state:

- `solved=true`
- `replay_verified=true`
- `endwalk=255`
- `goalcard=10`
- `powerup=0` (small Mario)

This settles the 1-2 fork: the tall pipe and terminal clear do not require Raccoon flight. The
wall fell to focused P-speed/LEFT+RIGHT movement, and the terminal blocker was a card approach
finisher.

## Ladder

Measured from `runs/sma4_cache/1-2_entry.pkl`:

- Surface actions: `x_max=629`, failed at the tall pipe.
  - Artifact: `runs/20260626-195202-sma4_snapshot_1_2_surface/`
- Full physics action set: `x_max=630`, no meaningful improvement.
  - Artifact: `runs/20260626-195917-sma4_snapshot_1_2_physics/`
- Focused P-speed/LEFT+RIGHT action set: `x_max=2803`, reaches the end-card area but does not
  terminal-clear.
  - Artifact: `runs/20260626-201118-sma4_snapshot_1_2_pspeed/`
- Patched finisher: terminal clear.
  - Artifact: `data/solutions/sma4/1-2.json`

The useful affordance was focused P-speed control, not broad action-set breadth.

## Finisher Change

`mario/search.py::goal_suffix_search` now:

- replays the candidate path frame-by-frame and stores exact frame-level prefixes with snapshots,
- keeps post-run settle snapshots after the progress coordinate has plateaued,
- tries a cheap `card_return_jump` template before the broader run/jump/brake templates.

The 1-2 suffix found by the generalized finisher:

- snapshot reason: `post_settle`
- cut frame: `808`
- return action: `LEFT+B`
- return frames: `50`
- jump action: `RIGHT+A`
- hold frames: `14`
- tail action: `LEFT`
- success frame: `97`

This matches the visual diagnosis: the P-speed prefix overshoots the floating card, then the
agent must walk back left under the card and jump into it.

## Code and Tests

Changed:

- `mario/adapters.py`
  - Added `LEFT+A`, `LEFT+A+B`, `A`, and `A+B` to the focused `SMA4_PSPEED_ACTIONS` set. These are
    appended so existing saved action indices remain valid.
- `mario/search.py`
  - Added post-settle snapshots and the `card_return_jump` fast path to `goal_suffix_search`.
- `tests/test_adapter_search.py`
  - Added `ReturnCardAdapter`, a non-ROM regression for the overshoot/settle/return/jump shape.
  - Strengthened action-set coverage for focused left-jump controls.

Verification:

- `./venv/bin/python -m pytest --disable-warnings`
  - `67 passed, 10 skipped`
- `MARIO_AI_SMA4_ROM='roms/Super Mario Advance 4 - Super Mario Bros. 3 (USA, Australia) (Rev 1).gba' ./venv/bin/python -m pytest -q tests/test_sma4_overworld.py`
  - `5 passed`
- `git diff --check`
  - clean

## Progress Coordinate Audit

Karisa Advynia's SMA4 disassembly was cloned to `/tmp/sma4-disasm-codex` for local text search.
The relevant code around `08014D84` labels the value written at `[03007818]` as player X position,
and the current adapter address `0x03003F24` is the corresponding 32-bit fixed-point player X
coordinate. That makes the repeated `x≈2800` in 1-1/1-2 more likely to be the end-card barrier
coordinate for these W1 card levels, not an 8-bit representation cap.

IWRAM diffing over the solved 1-2 approach found a plausible camera-like candidate:

- `0x03003894` rises with rightward progress and stays pinned near `2695` while Mario walks back
  left at the end barrier.
- It shifts during the card jump/end routine and is not labeled clearly enough in the disassembly,
  so it was not promoted to adapter info or `progress()`.

Conclusion: keep `progress()` on confirmed player X for now, and rely on the goal/card finisher
near capped end-card states. A true long-level probe is still needed before a broad W2-W8 sweep.

## Next

Run a telemetry-backed World-1 sweep using the replay-verified 1-1 and 1-2 clears to discover and
attempt 1-3, fortress, and airship/boss. Expect 1-3/whistle or fortress to be the first point where
flight or a real meta-planning decision matters.
