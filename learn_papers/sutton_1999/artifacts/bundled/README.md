# Bundled artifact snapshots

Optional: commit small `summary.json` + `episodes.csv` subsets here so figures reproduce
without re-running 2000-episode training.

Naming: `exp01_reinforce_summary.json`
