# Mario AI — Working Memory (CLAUDE.md)

> Ground truth lives in `runs/status.json` (regenerated by `scripts/update_status.py`).
> This file is the curated narrative. NEVER hand-edit the STATUS block — run the script.
> Follow the **Self-healing rules** at the bottom every iteration. Architecture: `DESIGN.md`.
> Build/verification plan: approved plan (search-teacher → distill → DAgger → value+speed).
> Session notes live in `notes/`: update `notes/session-log.md`, relevant
> `notes/sessions/*.md`, and `notes/research-bibliography.md` when an
> experiment or research pass changes what we know.

## Current status (one line)
**Full game beaten by SEARCH (any% 8/8 `beat_game=True`); 31/32 stock levels solved. V6 (`V6_FINDINGS.md`) settled the learning paradigm from first principles: the net SERVES the search (AlphaZero-with-a-real-sim) — policy-guided beam cut 1-1 search 2.53× (7005→2770 nodes) at top-3 while still solving; standalone IL control is the real ceiling (caps ~0.45-0.5, DAgger degrades it, reproduced — not methodology). V5 (`V5_FINDINGS.md`) 2-premise CORRECTION still holds: zero-shot generalist Mario needs ~10³-10⁴ levels (Procgen), not ~32 (the manipulation scaling law doesn't transfer), AND offline BC/DAgger is the wrong paradigm given a resettable sim+oracle — the pivot is reverse-curriculum RL (`scripts/rc_rl.py`, directionally validated: solves the last 25-40% of 1-1 where BC=0, but plain DQN hits plasticity loss). Per-level deliverable stays search-solution + rescue.** 8-4's escape is a 4-pipe chain through CastleArea6 (each down-pipe bypasses a `ProcLoopCommand` Y=240 gate): pipe-1 x1296→pg7, pipe-2 x2432 (upper, Y64)→pg12, pipe-3 x3648→water `$02`, then the water SIDE-pipe (`$6c`, the 2-2 recipe) → pg16 → Bowser bridge → axe. Cached `data/solutions/8-4.json` (2271 cf1 frames, `by=downpipe_chain`); `render.replay` beat=True (flag x=4821); contact sheet shows all legs + Bowser. Root cause of the whole 8-4 saga (4 prior false negatives) = a DETECTOR BLIND SPOT: gym fast-forwards pipe transitions INSIDE `env.step`, so post-step `$0760/$06DE/$000E` is blind — the correct signal is a per-frame live-RAM x-jump >100 (or raw `sim.u._env.frame_advance`). Codex disassembly consult + my own SMBDIS.ASM audit decoded the full CastleArea6 pipe map; legs solved by focused Go-Explore + descent-DOWN macros. ALL hard castles now solved: **4-4** (height-maze, Y64→Y176 descent goes LEFT off the platform edge — balanced-action Go-Explore) and **7-4** (the single hardest level — 6-gate multi-loop, decomposed into per-triplet sub-solves from a clean boundary via a Codex deep-dive, independently re-verified). 16 levels solved; **any% 8/8 `beat_game=True`**; every hard castle/water level (1-4,2-2,4-4,7-2,7-4,8-4) beaten. **V4 distillation attempted + consolidated (`V4_FINDINGS.md`):** the search teacher is excellent but distilling it into a *policy* is the wall — a flat MLP generalist = 0/11, a tiny 152K entity-transformer (the structured-state idea) is a BETTER representation (val-acc 0.65>0.57, actually plays to 35–77%) yet still 0/11 closed-loop; generalist DAgger *oscillates* (multi-task interference: 1-1 35→77→35), focused-specialist DAgger plateaus at the first pit. Conclusion: representation is NOT the bottleneck — robust closed-loop control from thin data is; the proven learned-agent path is per-level specialists + search-rescue. Also: novelty/literature + cross-game ("any Mario game") feasibility analysis in `V4_FINDINGS.md`.

<!-- STATUS:BEGIN (generated by scripts/update_status.py — DO NOT EDIT BY HAND) -->
| Milestone | Status | Key metric |
|---|---|---|
| V0 beat 1-1 (search) | DONE | beat=True, framerule=52, deaths=0 (20260603-130253-v0_1_1) |
| V1 dataset | DONE | n_samples=8748, levels_beaten=11 |
| V2 BC clears 1-1 | DONE | completion_rate=None (20260603-230433-v2_eval) |
| V3 DAgger | DONE | completion_rate=1.0 (20260603-204027-v2_eval) |
| V4 multi-world | TODO | — |
| V5 value+speed | TODO | — |

Current best: 1-1: beat (fr=52) net=0%; 1-2: net=0%; 1-3: net=0%; 1-4: net=0%; 2-1: net=0%; 4-1: net=0%; 4-2: net=0%; 4-4: net=0%; 8-1: net=0%; 8-2: net=0%; 8-3: net=0%. Tests: 49 passed / 0 failed. Bench: 1375.7 fps, snapshot 73.076µs, 336.9 nodes/s. _(generated 2026-07-15T05:44:27Z, 22c9022-dirty)_
<!-- STATUS:END -->

## Cross-game status
- 2026-06-26: The adapter path now reaches beyond NES SMB1. SML has a PyBoy-backed adapter/solver path, and SMA4 / SMB3 1-1 has a Stable-Retro/mGBA adapter with a replay-verified local solution. The SMA4 result is `data/solutions/sma4/1-1.json` (`solved=true`, `replay_verified=true`) with contact sheet `runs/sma4_1_1_solved_contact.png`. Detailed notes: `notes/sessions/2026-06-26-cross-game-sma4.md`.
- 2026-06-26: **SMB3 (SMA4) is now the active direction — a two-tier emulator-backed search agent** (overworld meta-planner + per-level search as an option). M0 (overworld RAM + `SMA4OverworldAdapter`), M1 (generalized `boot_to_level` + `goal_suffix_search` + adapter viz), and the **M2 meta-loop** (`overworld_solve`: solve → map advances → discover next gated level — proven on 1-1→1-2) all DONE. Remaining for full World 1: hard-level search (`coverage_search` adapter port) + fortress/boss. See `notes/sessions/2026-06-26-smb3-overworld-foundation.md` and memory `mario-smb3-direction`.
- 2026-06-27: SMA4 1-2 is terminal-cleared and replay-verified without flight. The ladder stands: surface actions fail at `x_max=629` (`runs/20260626-195202-sma4_snapshot_1_2_surface/`), full physics reaches only `x_max=630` (`runs/20260626-195917-sma4_snapshot_1_2_physics/`), focused P-speed/LEFT+RIGHT reaches `x_max=2803` (`runs/20260626-201118-sma4_snapshot_1_2_pspeed/`), and the generalized card finisher closes the level by settling at the cap, walking back left, and jumping into the panel (`data/solutions/sma4/1-2.json`, `runs/20260627-104611-sma4_snapshot_1_2_pspeed_finish/solved_contact.png`). Details: `notes/sessions/2026-06-27-sma4-1-2-finish.md`.
- 2026-06-27: Cached World-1 fast-forward works: replay cached 1-1/1-2 entry snapshots → settle to post-1-2 map → discover/enter 1-3. 1-3 normal route reaches the roulette-card area (`x_max=2546`, `goalcard=2`, `endwalk=0`) and currently misses the card; this is a terminal finisher/timing wall, not flight. The known 1-3 whistle remains a route-data/subgoal trick (white-block duck), not pursued in this pass. The `(160,64)` side node was also entered and stalls at an early sky-platform gap (`x_max=329`); it does not visually look like a fortress. Details: `notes/sessions/2026-06-27-sma4-world1-sweep.md`.
- 2026-06-29: The SMB3 direction is now an **Option-MDP / whistle-planning benchmark**, not more World-1 grinding. `mario/options.py` adds `MetaState`, `Option`, `OptionLibrary`, `KnowledgeTier`, resettable opaque-effect `search_options`, and wrappers for `data/solutions/sma4/{1-1,1-2}.json` as Tier-0 `ClearLevel` options. `scripts/probe_sma4_whistle.py` confirms the Tier-1 spend path: one whistle reaches the special warp-zone map (raw world byte `8`), a second whistle opens the 5-8 warp-zone screen, `RIGHT`+`A` selects the World-8 pipe, and raw world byte `7` confirms World 8. Passing artifact: `runs/20260629-130158-sma4-whistle-probe/report.json`; detailed note: `notes/sessions/2026-06-29-option-mdp-whistle-benchmark.md`.
- 2026-06-29: **ROM-backed whistle substitution DONE.** `scripts/bench_sma4_whistle_rom.py` runs greedy/BFS/uniform-cost on a library where `grant_two_whistles` is Tier-1 injected but `use_whistle`, `use_whistle_again`, and `select_world8_pipe` are real effect-opaque Stable-Retro/mGBA transitions. Default artifact `runs/20260629-202831-sma4-whistle-rom-bench/report.json`: greedy does not discover the skip; uniform-cost observes raw `8` (warp-zone), raw `8` (5-8 screen), then raw `7` (World 8) and returns a 7.75x cheaper plan. Blocked-route artifact `runs/20260629-202846-sma4-whistle-rom-bench/report.json`: greedy stuck, search still skips.
- 2026-07-14: **Bottom-up research plan for the SMA4/SMB3 Option-SMDP track** (theory audit + experiment ladder). Note: `notes/sessions/2026-07-14-bottom-up-research-plan.md`. Challenges: whistle planner gap still Tier-1 inventory + symbolic warpless/Bowser; Foster/Song nuance IL ceiling; no SMA4 throughput bench yet; Φ/card-cap unfinished. Critical path: A0 bench → A2 white-block RAM → B2 AcquireWhistle → B4 rebench.
- 2026-07-15: **Tier-2 `AcquireWhistle_1_3` DONE + rebench without first hand-grant.** Behind-bg RAM `0x03003D06`; P-Wing entry required (small Mario cannot duck-activate); SMA4 Toad chests open with **B**. Solution `data/solutions/sma4/acquire_whistle_1_3.json` (2533f, replay_verified). `--no-hand-grant` ROM bench: search 5 hops / 12099 via acquire→use→…; greedy warpless 69000 → **5.7×** (`runs/20260715-sma4-whistle-rom-bench-acquire-open/report.json`); warpless-blocked greedy stuck / search still skips. A0: `bench/sma4_{step_rate,snapshot_cost}.json`. Note: `notes/sessions/2026-07-15-acquire-whistle-1-3.md`.

## What works / verified  (each line MUST cite a backing artifact)
- Native arm64 env: torch 2.12 MPS available; gym-super-mario-bros 8.0 / nes-py 9.0 — smoke test passed.
- Determinism + snapshot exactness — `tests/test_determinism.py`, `tests/test_snapshot.py` GREEN (golden committed).
- Death-aware reward invariants — `tests/test_reward.py` GREEN (death dominant, fake-progress counters ignored).
- Artifact contract — `tests/test_artifacts_schema.py` GREEN; `schemas/result.schema.json` locked.
- Emulator throughput measured — `bench/step_rate.json`, `bench/snapshot_cost.json`.
- V0: beam search beats 1-1 — `runs/20260603-130253-v0_1_1/result.json` (pass=True); flagpole visible in its `contact_sheet.png`.
- Tile observation extractor — visually verified (`scripts/verify_observation.py`: ground/pit/stairs align); `tests/test_observation.py` GREEN.
- Self-healing tracking spine works — `update_status.py` regenerated the STATUS block from `runs/status.json`; `verify_iteration.py` runs the loop.
- `label_state` teacher primitive validated — soft targets sharp at hazards (fatal→0, jumps dominate), uniform on open ground; `tests/test_label.py` GREEN. Drove the design: HARD label primary (decisive search action), SOFT auxiliary (entropy-weighted in the loss).
- V1 dataset (1-1) passes its gate — `data/manifest.json` (on-path + perturb-and-recover coverage); `scripts/verify_dataset.py` PASS. K-stacking trajectory-aware (`tests/test_buffer.py` GREEN).
- V2 BC pipeline runs — trained `MarioPolicy` (1.85M params); CPU-vs-MPS parity GREEN (`bench/mps_parity.json`: argmax agreement 1.0, logit diff 2e-6); eval harness + `eval.json` contract (`tests/test_eval_schema.py` GREEN).
- **A learned net beats 1-1 (30/30)** — `runs/20260603-204027-v2_eval/eval.json` (V3, completion 1.0); flagpole visible in its `contact_sheet.png`. Distilled from search + warm-start DAgger.
- Observation timing sensors (gap_ahead pit-distance + x_subtile) were the unlock for the first pit — `tests/test_observation.py::test_gap_ahead_logic` GREEN.
- Multi-stage runner (`mario/runner.py`) + `search_from_state` play continuously, net-primary + live search-rescue; per-level specialists (1-1, 1-2) exist but DON'T transfer to the multi-stage level-start (specialists are single-stage), so the run uses net on 1-1 + search elsewhere.
- Continuous playthrough delivered — `runs/20260604-013306-playthrough_w1plus/full_run.mp4`: 1-1 (net) + 1-2,1-3,1-4,2-1 (search), every level reaches the flag. `solve_and_stitch.py` solves levels offline (no lives limit) + stitches; `scripts/probe_level.py` renders any level's search attempt.
- `coverage_search` (`mario/search.py`) — general solver = beam + Go-Explore coverage (dedup on the cell `(area,x-tile,y-tile)` + per-lineage novelty bonus, area-FIRST progress). Cracked **4-2's vertical/warp-zone maze** where plain beam's beam emptied (x3011); `data/solutions/4-2.json` solved=True. Optional `area_bonus`/`ground_bonus`/`loop_back_px` knobs for maze levels.
- **any% FULL GAME beaten (8/8, `beat_game=True`)** — `data/solutions/{1-1,1-2,4-1,4-2,8-1,8-2,8-3,8-4}.json` all solved=True (flag reached); `scripts/stitch_solutions.py "any%"` → `runs/playthrough_anypct/{full_run.mp4,playthrough_result.json}` with `beat_game=True`, `levels_beaten=8/8` (1-1 by the learned net, 8-4 by the down-pipe chain, rest by beam/coverage). 14 distinct levels solved total.
- **8-4 SOLVED (the final any% blocker) — disassembly-decoded 4-pipe chain through CastleArea6.** `data/solutions/8-4.json` (2271 cf1 frames, `by=downpipe_chain`); `render.replay` beat=True (flag x=4821, no death); `runs/8-4_solved_contact.png` shows all legs + Bowser bridge. Route = pipe-1 (p5c1r6 x1296→pg7) → pipe-2 (p9c8r4 x2432, UPPER Y64→pg12) → pipe-3 (p14c4r6 x3648→water `$02`) → water SIDE-pipe (`$6c`, the 2-2 recipe → pg16) → Bowser bridge beam → axe (`flag_get`). Each down-pipe bypasses a `ProcLoopCommand` Y=240-grounded gate (pages `$06/$0b/$10`). Scripts `solve_84_{leg3,leg4,finish}.py` + the verified prefix files; entry detector = per-frame live-RAM x-jump >100 (raw access = `sim.u._env.frame_advance`, NOT `sim.u.step`).
- **7-4 SOLVED (warpless castle, the single hardest level) — 6-gate multi-loop cracked.** `data/solutions/7-4.json` (2913 cf1, `by=codex_multiloop_audited`); `render.replay` beat=True (flag x=3274, no death); `runs/7-4_solved_contact.png` shows the maze → Bowser → axe. 7-4 = CastleArea5 ($64), world-7 `ProcLoopCommand` MULTI-loop: 2 triplets (pages 4/5/6 then 8/9/a) each needing 3 consecutive checkpoints at the correct `$00CE` grounded (`MultiLoopCorrectCntr $06d9`); gate fires on the RENDERER page/col (`$0725/$0726`). Solved by DECOMPOSING into per-triplet segmented search from a clean (`mlp=0`) boundary (Codex deep-dive, raw `frame_advance` gate tracing) + a Bowser-finish search; I independently re-verified (path==raw_actions, run_chunk replay beat=True frame 2912, 6 correct gate firings, no loopbacks). My own Go-Explore variants had stalled (doomed tainted-`mlp` seeds; counter-aware cell-key state-explosion).
- **4-4 SOLVED (warpless castle) — height-maze via the lower-path descent.** `data/solutions/4-4.json` (221 cf8, `by=castle_lowerpath`); `render.replay` beat=True (flag x=2769, no death); `runs/4-4_solved_contact.png`. 4-4 = CastleArea2 ($61), NO pipes, pure height-nav through 2 `ProcLoopCommand` gates: page 5 needs `$00CE`=64 (upper) grounded, page 9 needs `$00CE`=176 (lower). KEY unlock: the Y64→Y176 descent requires going **LEFT** off the upper platform's edge — 3 right-biased searches (incl. Codex) all missed it; fixed with a BALANCED action set (left+down) + reward for grounded Y176. `solve_castle.py` (gate table from SMBDIS.ASM:7787), `solve_44_descent.py`, `solve_44_lower.py`.
- 8-4 page16→12 loop diagnosed from RAM (`mario/ram.py` addrs): at x≈3847 page=16 Y=108 float=1 (airborne) area=3, pushing right teleports page 16→12 (the SMB ProcLoopCommand back-4-pages loop). This loop is a **DECOY**, not the route — the search stalled there only because it walked PAST the three castle DOWN-pipes (x1296/2432/3648) without grounding+pressing DOWN on the pipe-top. (Superseded the earlier "NO area change reachable" claim, which was a walk-past-the-pipe + detector artifact.)
- **Global progress coordinate Φ (the Jun-4 audit's #1 root-cause fix) — DONE + verified.** `beam_search` now scores level-GLOBAL progress Φ=`area_seq*10000 + (x - area_entry_x)` (via `state_score(progress=)`) with an area-aware `_dedup_key`, so a pipe/area transition reads as forward progress instead of a −1000px collapse. Regression-clean: 1-1 solved (len134) and the multi-area 1-2 solved (len138). Root cause was `x_pos` being area-local (`smb_env.py:149`).
- **$0750 area-pointer fix + room-graph tooling (Codex-confirmed).** Sub-area/pipe transitions flip `$0750` (AreaPointer), NOT `$0760` (verified 1-2/4-2). `area_search` (`mario/search.py`) keys success+cell on `$0750` with `forbid_keys`/`max_x`/`actions`; `solve_area_chain.py` is a room-graph DFS. CAVEAT (Codex): a bare `$0750` flip is a pending pipe-DESTINATION marker (set by row-`$0e` objects), NOT a transition — a REAL entry needs `ChangeAreaTimer $06DE` nonzero / `GameEngineSubroutine $000E`→pipe mode. 8-4 decoded route: markers `($e5,$07)→($65,$0c)→($02,$00 water)→($65,$10 Bowser)`, reached via 3 castle DOWN-pipes (x1296→pg7, x2432→pg12, x3648→water $02) entered by a DOWN-held descent onto the pipe-top (10/11 row6), then a water SIDE-pipe → Bowser → axe. (Superseded the earlier "lethal lava mount → TAS-only" claim — leg 3 is a normal down-pipe, confirmed empirically.) `recon_8_4.py`/`pipe_sweep_8_4.py`/`verify_route_8_4.py` tooling.
- **Policy-guided search ("net serves the search", V6) — DONE + measured.** `beam_search(policy_prior=, policy_weight=, policy_topk=)` + `mario/entity_policy.py::EntityPolicyPrior` score log π(a|s) per node and expand only the prior's top-k actions (opt-in; default off; 49 tests green). On 1-1 with even a weak 0.19-standalone prior: **plain width-6 beam 7005 nodes/66.5s → guided (top-3) 2770 nodes/30.8s, both solved = 2.53× node reduction** (`scripts/policy_guided_search.py`; top-2 over-prunes a weak prior → use top-3). The literature-blessed deliverable: search solves, the learned net accelerates. Contrast: **standalone IL control is the real ceiling** — `scripts/exit_specialist.py` (deep-teacher + hazard focal loss + from-scratch balanced DAgger) caps 1-1 at ~0.45-0.5 and DAgger DEGRADES it (`V6_FINDINGS.md`), reproduced across recipes (not methodology). **Temporal context TESTED → no benefit** (`TemporalEntityTransformer`/`TemporalEntityPolicyPrior`, opt-in, 49 tests green): standalone ctx1=ctx4=0.36; prior K1=2.41× vs K4=2.14× — don't re-try architecture tweaks (V6 §5).
- **Value-guided best-first search (ExIt §10) — DONE.** Independent `ValueNet` (`mario/value.py`, val-AUC 0.921) trained from solved trajectories (`gen_value_data.py`/`train_value.py`); `beam_search(value_guide=,value_weight=)` adds `w·V(s)`. Functional (~10% overhead, never breaks solving) but NO node savings on our levels — Φ already solves them at width 5-6; value's benefit is reserved for deceptive levels where plain fails. (Value head kept INDEPENDENT because a shared-trunk BCE head regressed 1-1.)
- **2-2 AND 7-2 (underwater) SOLVED from scratch** — `data/solutions/{2-2,7-2}.json` solved=True; `render.replay` beat=True (both flag x_pos=3161); contact sheets show swim→side-pipe→surface area-2→flagpole. Entry = hold RIGHT into the side-pipe (descend to x3011/Y112 foot-landing frame → side-probe `(X+13)=3024`→`$6c`); 7-2 entry caught as `x_jump` by `real_transition`. KEY GOTCHA: gym fast-forwards pipe transitions inside `env.step()`, so detect entries by the post-step backward x-jump (`mario/ram.py::real_transition`), not `pipe_entering` after the wrapped step. `scripts/solve_22.py`/`solve_72.py`.
- **Corrected transition semantics — `mario/ram.py` + `area_search` (Codex disassembly-verified, tests GREEN).** `pipe_entering(ram)` = real pipe/area entry underway (`$06DE!=0` OR `$000E∈{2,3}`); `stage_key(ram)` = `($075f,$075c)` (water-level completion increments `$075c`, no flagpole); `$0750` documented as a PENDING-destination marker (not entry). `area_search` now succeeds on `transitioned()` = flag | pipe_entering | stage-change | new-room. Decoded 2-2's exit to the exact engine probe (`scripts/solve_22_entry.py` drives `(X+13,Y+24)`→pipe metatile). Snapshot/restore fidelity independently re-verified exact.
- **Cross-game adapter spine is working beyond SMB1.** `mario/adapters.py` defines `GameAdapter`, `SMLAdapter`, and `SMA4Adapter`; `mario/search.py::beam_search_adapter` searches through adapter snapshots; `tests/test_adapter_search.py` covers the adapter contract and SMA4 card-hit terminal state; `tests/test_sml_adapter.py` covers SML snapshot behavior when `MARIO_AI_SML_ROM` is set. SMA4 / SMB3 1-1 is locally solved and replay-verified via `data/solutions/sma4/1-1.json`; visual check at `runs/sma4_1_1_playthrough/contact_sheet.png`.
- **SMB3 (SMA4) overworld instrumentation + generalized level solver (two-tier M0+M1).** `SMA4OverworldAdapter` (`mario/adapters.py`) reads the reverse-engineered map cursor/world/progress RAM and shares one emulator core with the level view (snapshots interchangeable); `tests/test_sma4_overworld.py` (5, ROM-gated) GREEN incl. the overworld→level transition. `scripts/probe_overworld.py --clear-1-1` reproduces/asserts the addresses. The level-agnostic `mario/search.py::goal_suffix_search` replaces the hardcoded 1-1 card window — 1-1 re-solved via it (`data/solutions/sma4/1-1.json`, `solver=beam_search_adapter+goal_suffix`, `solved=true`/`replay_verified=true`); visual clear at `runs/sma4_1_1_solved_contact.png`. Adapter-backed `mario/render.py::{replay_adapter,make_contact_sheet_adapter}` + `scripts/replay_video_adapter.py`; non-ROM coverage in `tests/test_adapter_search.py` (CappedGoalAdapter).
- **SMB3 (SMA4) two-tier meta-loop works end-to-end (M2).** `mario/overworld_search.py::{discover_nodes,settle_to_map,overworld_solve}` + `scripts/solve_sma4_world.py`; run `runs/20260626-180705-sma4_world/world_summary.json` shows 1-1 solved FRESH from the overworld entry (x_max=2803), the map then advancing so 1-2 `(128,32)` is discovered + enterable (gating verified). `tests/test_overworld_search.py` (non-ROM, GREEN) covers discovery + the post-clear settle. `update_status.py` is game_id-keyed.
- **Adapter Go-Explore (`mario/search.py::coverage_search_adapter`).** Lean adapter-generic coverage (coarse `(x//16,y//16)` cell archive + one-time novelty bonus; degrades to beam on open ground), wired into `overworld_solve` as the beam→coverage→`goal_suffix` escalation. Non-ROM tests GREEN (`tests/test_adapter_search.py`: solves FakeAdapter + CappedGoalAdapter). NOTE: does NOT crack 1-2's tall-pipe wall (x=629) — that's a per-level route/flight problem, not a search-breadth one.
- **SMA4 1-2 SOLVED from the overworld entry snapshot; the wall was P-speed, the finish was card recovery, not flight.** Ladder from `runs/sma4_cache/1-2_entry.pkl`: surface actions fail at `x_max=629` (`runs/20260626-195202-sma4_snapshot_1_2_surface/`), full physics reaches `x_max=630` (`runs/20260626-195917-sma4_snapshot_1_2_physics/`), focused P-speed/LEFT+RIGHT reaches `x_max=2803` (`runs/20260626-201118-sma4_snapshot_1_2_pspeed/`). The generalized `goal_suffix_search` now records frame-level prefixes plus post-cap settle snapshots and tries a card-return template; 1-2 closes with `LEFT+B` return 50f → `RIGHT+A` jump 14f → `LEFT` tail, `endwalk=255`, `replay_verified=true` (`data/solutions/sma4/1-2.json`; contact sheet `runs/20260627-104611-sma4_snapshot_1_2_pspeed_finish/solved_contact.png`). `scripts/probe_sma4_physics.py` confirms P-speed/speed/powerup fields; `0x03002C52` is NOT yet a validated clear bitmap.
- **Option-MDP scaffold for the whistle-planning benchmark is in place.** `mario/options.py` defines `MetaState(world,node,cleared,inventory)`, `KnowledgeTier`, `Option`, `OptionLibrary`, resettable opaque-effect `search_options`, and `SMA4SolutionExecutor`; `tests/test_options.py` proves tier gating and an opaque-effect whistle shortcut that must be executed/observed before it can be exploited. Existing `data/solutions/sma4/{1-1,1-2}.json` wrap as verified Tier-0 `ClearLevel` options. Verification: `pytest --disable-warnings` -> 72 passed / 10 skipped.
- **SMA4 whistle spend plumbing is feasible, with caveats.** `scripts/probe_sma4_whistle.py` hand-grants whistles using inventory slots `0x03002C2E..0x03002C51` (`0x0C` = warp whistle), opens inventory with `L`, uses the selected item with `A`, and reaches World 8 through the two-whistle route. Passing artifact `runs/20260629-130158-sma4-whistle-probe/report.json`: raw world byte `8` = warp-zone special map; raw `7` = World 8. Caveat: this is Tier-1 hand-granted inventory, not real acquisition; the adapter mode classifier now special-cases raw worlds `7/8` as map-like off-grid states.
- **Phase 3 planner-class contrast — the meta-intelligence measurement (the thesis test).** `mario/meta_planner.py` (`greedy_plan` myopic baseline; cost-optimal `search_options_uniform_cost`; symbolic `build_whistle_benchmark_library`/`run_whistle_benchmark`) + `scripts/bench_whistle_planning.py` + `tests/test_meta_planner.py` (6 GREEN). On the SAME effect-opaque whistle library: **greedy completes the game the long warpless way but NEVER discovers the whistle skip** (8 hops, skip=False), while resettable search **discovers the opaque `use_whistle→World 8` payoff and returns a 10.95× cheaper plan** (3 hops); `--warpless-blocked` makes greedy STUCK (found=False) while search still skips. Artifact `runs/20260629-175809-whistle-planning-bench/report.json`. Costs are symbolic planning-layer stand-ins (option endpoints + whistle spend ROM-verified separately); Tier-1 hand-grant recorded as injected facts (`whistle_hand_granted`,`whistle_in_inventory`).
- **ROM-backed whistle substitution validates the same planner-class gap with real emulator effects.** `mario/options.py::SMA4WhistleExecutor` + `mario/meta_planner.py::build_sma4_whistle_rom_library` + `scripts/bench_sma4_whistle_rom.py`: greedy takes the symbolic warpless route (8 hops / 69000 frames / skip=False), while uniform-cost executes real opaque options and observes `use_whistle` -> raw `8` cursor `[64,80]`, `use_whistle_again` -> raw `8` cursor `[128,144]`, `select_world8_pipe` -> raw `7` cursor `[32,80]`, mode `overworld`; plan is 5 hops / 8900 frames / skip=True = 7.75x cheaper (`runs/20260629-202831-sma4-whistle-rom-bench/report.json`). With `--warpless-blocked`, greedy is stuck and search still skips (`runs/20260629-202846-sma4-whistle-rom-bench/report.json`). Verification: `pytest --disable-warnings` -> 80 passed / 10 skipped; ROM-gated SMA4 overworld -> 5 passed.
- **SMA4 M2 throughput bench (A0).** `scripts/bench_sma4.py` → `bench/sma4_step_rate.json` / `bench/sma4_snapshot_cost.json` (~934 fps, ~550µs snapshot RT, ~230 nodes/s @ chunk=4).
- **Tier-2 `AcquireWhistle_1_3` replay-verified (B2).** White-block duck via behind-bg `0x03003D06`, Toad chest opens with **B**, inventory `0x0C` at `0x03002C2E`; `data/solutions/sma4/acquire_whistle_1_3.json` (2533 frames, `replay_verified=true`); visual `runs/20260715-acquire-whistle-e2e/whistle.png`. Executor: `SMA4WhistleExecutor.acquire_whistle_1_3`; tests: `tests/test_acquire_whistle_solution.py`.
- **B4 rebench without first hand-grant.** `scripts/bench_sma4_whistle_rom.py --no-hand-grant`: greedy warpless 69000 / skip=False; search `acquire_whistle_1_3→…→clear_bowser` 12099 / skip=True = **5.7×** (`runs/20260715-sma4-whistle-rom-bench-acquire-open/report.json`); `--warpless-blocked` greedy stuck / search still skips. Remaining injections: P-Wing entry snapshot, second-whistle regrant, MAP_CURSOR sync after acquire exit.

## Active issues  (open only — move to changelog when resolved)
- [ ] **6-2 is the ONLY unsolved stock level** (31/32 solved this session via `solve_all_stock.py`). beam+coverage exhausted at 1444s; it's the moving-lift/athletic level. Retry with tuned `coverage_search` knobs or `solve_castle`-style Go-Explore if pursuing 32/32. (6-3 also caches `solved=True` but is a determinism FALSE POSITIVE — replay at seed 0 dies; the dataset builder's replay gate excludes it. Re-solve at seed 0 or quarantine.)
- [ ] **Generalist zero-shot policy is OPEN; standalone learned control is the wall (V5+V6).** Game-level generalization needs ~10³-10⁴ levels (Procgen), not ~32. V6 then showed standalone IL control caps ~0.45-0.5 even done right (deep teacher + hazard loss + balanced DAgger) and DAgger degrades it — so the productive path is NOT a standalone controller but **the net as a search prior** (policy-guided beam, DONE, 2.53×) and, only for an irrecoverable last-fraction, reverse-curriculum RL (`scripts/rc_rl.py`, directionally validated, plain DQN plasticity loss). True generalization needs a PCG pipeline + the NES custom-level engine wall (stock-ROM-locked).
- [ ] **SMB3/SMA4 Option-SMDP: first whistle is Tier-2; climb the rest of the knowledge ladder.** Still injected: P-Wing 1-3 entry snapshot, second-whistle warp-zone regrant, MAP_CURSOR sync after acquire exit; warpless/Bowser symbolic. Next honesty wins: fortress `AcquireWhistle` + map P-Wing initiation option.
- [ ] Specialists don't transfer single-stage→multi-stage (enemy frame-parity/start differ); only 1-1 (identical first level) transfers.

## Next action  (exactly ONE, concrete)
**Build the second W1 `AcquireWhistle` (fortress flight / hidden door), replay-verify it, and remove `whistle_regranted_in_warp_zone` from the ROM-backed whistle library.**

## Notes / gotchas  (durable facts, not status)
- Theory spine (textbooks → repo map): `notes/theory/first-principles.md`; re-fetch PDFs with `notes/theory/fetch_corpus.py` (pdfs gitignored).
- Use the native arm64 venv (`uv venv --python 3.13 venv`). The `/usr/local` python3.13 is x86_64/Rosetta and disables torch MPS — never use it.
- Run scripts with `PYTORCH_ENABLE_MPS_FALLBACK=1`; keep `torch.compile` / `detect_anomaly` OFF.
- nes-py ≈ 1380 fps/core (verified). Snapshots are NOT picklable → one beam search is in-process; parallelize ACROSS searches (levels/states), not within.
- Reward: `time` weight stays ~0 mid-level (21-frame framerules); raise only for the speed phase / 8-4.
- Determinism tests are torch-free (CPU/RAM) so MPS nondeterminism can't make them flaky.
- Working notes live in `notes/`. Use `notes/session-log.md` as the chronological index and put detailed experiment records in `notes/sessions/YYYY-MM-DD-topic.md`.
- Consolidated research references live in `notes/research-bibliography.md`; update it when citing new papers/docs.

## Recent changelog  (HARD CAP: last 10 entries, newest first)
- 2026-07-15: **First-principles theory corpus + synthesis.** Downloaded/read Sutton & Barto, Bertsekas (RL course / Lessons from AlphaZero / Rollout), Puterman–Chan draft, plus options/shaping/DAgger/ExIt/AlphaZero/Go-Explore. Durable map: `notes/theory/first-principles.md`; fetch script + README under `notes/theory/`. Textbook-grounds search-first + Option-SMDP + “net = Newton starting point.” Note: `notes/sessions/2026-07-15-first-principles-theory.md`.
- 2026-07-15: **AcquireWhistle_1_3 + B4 rebench.** A0 SMA4 bench (`bench/sma4_*.json`). White-block behind-bg `0x03003D06`; P-Wing entry; chest opens with B; `data/solutions/sma4/acquire_whistle_1_3.json` replay-verified (2533f, inv `0x0C`). `--no-hand-grant` ROM bench: search skip 12099 vs greedy warpless 69000 = 5.7×; warpless-blocked greedy stuck / search skips. Remaining: second-whistle regrant, P-Wing entry injection, cursor sync. Note: `notes/sessions/2026-07-15-acquire-whistle-1-3.md`.
- 2026-07-14: **Bottom-up theory audit + SMA4/SMB3 experiment ladder.** Deep dive from deterministic dynamics → potential shaping → Options/SMDPs → ExIt → IL (Foster NeurIPS'24, Song NeurIPS'25) → Go-Explore. Adversarial review of V5/V6/whistle findings; whistle planner-class gap challenged as incomplete (Tier-1 grant + symbolic endpoints). Plan phases A–F in `notes/sessions/2026-07-14-bottom-up-research-plan.md`; critical path A0 bench → AcquireWhistle B2 → rebench B4. Deprioritize flat generalist / in-loop LLM / world models. Bibliography + session-log updated.
- 2026-06-29: **Phase 3 planner-class contrast — the meta-intelligence measurement (Claude pass).** Built the planning-layer benchmark that actually tests the thesis ("how little meta-intelligence does any%-via-whistle need once control is exact"): `mario/meta_planner.py` (`greedy_plan` myopic hill-climb baseline that won't explore opaque-effect options; cost-optimal `search_options_uniform_cost`; symbolic `build_whistle_benchmark_library` + `run_whistle_benchmark`), `scripts/bench_whistle_planning.py`, `tests/test_meta_planner.py` (6). On the SAME effect-opaque whistle library: **greedy beats the game the long warpless way but NEVER discovers the 6-world whistle skip** (8 hops / 69000 symbolic frames, skip=False); resettable search **discovers the opaque `use_whistle→World 8` payoff and returns a 10.95× cheaper plan** (3 hops / 6300); `--warpless-blocked` → greedy STUCK (found=False), search still skips. Artifact `runs/20260629-175809-whistle-planning-bench/report.json`. Honesty: costs are symbolic planning-layer stand-ins (option endpoints + whistle spend ROM-verified separately); Tier-1 hand-grant recorded as injected facts. Tests: `pytest --disable-warnings` → 78 passed / 10 skipped. Note: `notes/sessions/2026-06-29-option-mdp-whistle-benchmark.md` (Phase 3 section). Also: monitoring own + Codex usage via Playwright tabs (load-balance: when one is rate-capped, route work to the other).
- 2026-06-29: **Option-MDP scaffold + SMA4 whistle plumbing recon.** Added `mario/options.py` (`MetaState`, `Option`, `OptionLibrary`, `KnowledgeTier`, resettable opaque-effect `search_options`, `SMA4SolutionExecutor`) and `tests/test_options.py`; fake tests prove tier gating and a whistle-like opaque shortcut that must be executed/observed before the planner can exploit it. Existing `data/solutions/sma4/{1-1,1-2}.json` now wrap as verified Tier-0 `ClearLevel` options. Added `scripts/probe_sma4_whistle.py`; passing artifact `runs/20260629-130158-sma4-whistle-probe/report.json` confirms the two-whistle spend path can reach World 8 from savestate with hand-granted inventory (`0x03002C2E..`, item `0x0C`; `L` opens menu, `A` uses; raw world `8` = warp-zone, raw `7` = World 8). Tests: `pytest --disable-warnings` → 72 passed / 10 skipped; ROM-gated SMA4 overworld → 5 passed. Note: `notes/sessions/2026-06-29-option-mdp-whistle-benchmark.md`.
- 2026-06-27: **Cached SMA4 World-1 sweep path works; 1-3 and the `(160,64)` side node classified.** Added cached replay helpers (`remap_solution_path`, `replay_cached_solution`, `fast_forward_cached_solutions`) plus `solve_sma4_world.py --fast-forward-solved --target-cursor --target-label`; cached 1-1/1-2 replay settles to the post-1-2 map (`runs/20260627-113025-sma4_world/fast_forward/`). New entry snapshots: `runs/sma4_cache/{1-1,1-3,1-fortress}_entry.pkl`. 1-3 normal route reaches the roulette-card area (`x_max=2546`, `goalcard=2`, small Mario) but misses `endwalk`; artifact `runs/20260627-113530-sma4_snapshot_1_3_pspeed/beam_best_contact.png`. Whistle route not pursued: white-block duck-through is route-data/subgoal behavior, not ordinary forward search. `(160,64)` side node stalls at a sky-platform gap (`x_max=329`), not visually a fortress; artifact `runs/20260627-115920-sma4_snapshot_1_fortress_pspeed/beam_best_contact.png`. Added non-ROM cached replay tests; `goal_suffix_search` now has a default time budget to avoid hanging diagnostic runs. Tests: `pytest --disable-warnings` → 69 passed / 10 skipped; ROM-gated SMA4 overworld → 5 passed.
- 2026-06-27: **SMA4 1-2 terminal-cleared and replay-verified; no flight required.** Added left-jump controls to `SMA4_PSPEED_ACTIONS`, taught `goal_suffix_search` to keep exact frame-level prefixes plus post-run settle snapshots, and added a cheap card-return template for capped SMB3 roulette-card finishes. The 1-2 finish is: P-speed prefix from `runs/20260626-201118-sma4_snapshot_1_2_pspeed/` → post-cap settle to frame 808 → `LEFT+B` 50f → `RIGHT+A` 14f → `LEFT` tail; final `endwalk=255`, `goalcard=10`, `replay_verified=true` in `data/solutions/sma4/1-2.json`; visual proof `runs/20260627-104611-sma4_snapshot_1_2_pspeed_finish/solved_contact.png`. Non-ROM regression added with a fake overshoot-return-card adapter. Progress-coordinate audit: Karisa SMA4 disassembly confirms `0x03003F24` is a 32-bit player X coordinate, not an 8-bit cap; `0x03003894` is a plausible camera-like candidate from RAM diffing but not validated enough to promote. Tests: `pytest --disable-warnings` → 67 passed / 10 skipped; ROM-gated `tests/test_sma4_overworld.py` → 5 passed.
- 2026-06-26: **SMA4 physics-state search: 1-2 wall artifact-backed and BROKEN without flight.** Added SMA4 physics telemetry (`pspeed`, speed, powerup, subpixels), richer opt-in action sets (`SMA4_PHYSICS_ACTIONS`, focused `SMA4_PSPEED_ACTIONS`), frame-macro execution, physics-aware `coverage_search_adapter` cells, coverage JSONL traces, partial contact sheets, `scripts/solve_sma4_snapshot.py`, and `scripts/probe_sma4_physics.py`. Ladder from `runs/sma4_cache/1-2_entry.pkl`: surface baseline fails at `x_max=629` (`runs/20260626-195202-sma4_snapshot_1_2_surface/`), full physics reaches only `x_max=630` (`runs/20260626-195917-sma4_snapshot_1_2_physics/`), focused P-speed/LEFT+RIGHT reaches `x_max=2803` (`runs/20260626-201118-sma4_snapshot_1_2_pspeed/`) — so 1-2's tall pipe is NOT a Raccoon-flight requirement. At this point terminal clear was still open (superseded by the 2026-06-27 card-return finisher). Probe artifact `runs/20260626-195128-sma4_physics_probe/physics_probe.json`; `0x03002C52` is not a validated clear bitmap. Updated `DESIGN.md` to stop rejecting Stable-Retro; added JaffarPlus/TAS/PokeAgent refs. Tests green (`pytest -q`; ROM-gated SMA4 overworld 5 passed with local ROM). Note: `notes/sessions/2026-06-26-sma4-physics-search.md`.
- 2026-06-26: **SMB3 (SMA4) two-tier agent — M0 + M1 + the M2 meta-loop DONE (overworld planner invoking per-level search).** The two-tier loop is PROVEN (`mario/overworld_search.py::overworld_solve`, run `runs/20260626-180705-sma4_world/`): discovered 1-1 → solved it FRESH from the overworld entry (x_max=2803, beam+goal_suffix) → the map ADVANCED → 1-2 discovered + enterable (gating works) → attempted 1-2 (stalled x=629, needs the `coverage_search` adapter port). `discover_nodes`/`settle_to_map`/`overworld_solve` + `scripts/solve_sma4_world.py` + `tests/test_overworld_search.py` (non-ROM, green); `update_status.py` made game_id-keyed. Also built `coverage_search_adapter` (adapter Go-Explore) wired as the beam→coverage→`goal_suffix` escalation. Fixed: cached frame-precise paths desync on re-entry (solve fresh per node, cache entry-snapshot+path); `enter_level` settles past the ~140-frame intro lockout. **1-2 found to be the first hard SMB3 level** (pipe-terrain, tall pipe at x=629 blocks ground search; beam+coverage both stall, finer chunks worse) → likely needs flight/powerup (M5). Started the SMB3-end-to-end direction (a hierarchical meta-game: overworld graph + consumable items + flight; see memory `mario-smb3-direction`). Reverse-engineered the SMA4 overworld RAM (`scripts/probe_overworld.py`, reproducible): map cursor X `0x03003DE4` / Y `0x03003DE0` on a 0x20 grid, world `0x03002A69` (0-indexed), progress `0x03002C52`; mode detection behavioral (level timer vs map cursor) with a documented post-clear results-screen false-positive. Built `SMA4OverworldAdapter` (shared-core view over `SMA4Adapter` — snapshots interchangeable; Stable-Retro is single-instance/process so this is mandatory), `boot_to_level`/`goto_node`/`enter_level`, and a level-agnostic `mario/search.py::goal_suffix_search` that REPLACES the hardcoded 1-1 card window (no `scroll_x` exists; x_pos plateaus at the goal, so the finisher is the fix). Re-solved 1-1 via the generalized pipeline (`data/solutions/sma4/1-1.json`, `solver=beam_search_adapter+goal_suffix`, replay_verified, `runs/sma4_1_1_solved_contact.png` shows LEVEL CLEARED). Added adapter-backed `replay_adapter`/`make_contact_sheet_adapter` + `replay_video_adapter.py`. Tests: `tests/test_sma4_overworld.py` (5, ROM-gated, green) + non-ROM finisher/viz coverage. Scope: M1's "solve 1-2" is gated behind 1-1; systematic node discovery + gated solving moved to M2. Note: `notes/sessions/2026-06-26-smb3-overworld-foundation.md`.
- 2026-06-26: **Cross-game adapter expansion + SMA4 / SMB3 1-1 solved.** Added the `notes/` session-log convention and root `AGENTS.md` for future agents. The adapter path now covers SMB1, SML/PyBoy, and SMA4/Stable-Retro-mGBA. SMA4 1-1 boots through title/map into gameplay, beam search reaches the roulette-card area, and a frame-level card finisher produces a replay-verified clear (`data/solutions/sma4/1-1.json`, `solver=beam_search_adapter+card_suffix`, `chunk_frames=1`). Critical finding: SMA4 end-level world `x_pos` caps near the boundary, so x-progress-only search runs under the card; the observed real card hit sets `endwalk=255`, so success is `endwalk != 0` while `goalcard` alone is not terminal. Visual artifacts: `runs/sma4_1_1_playthrough/{play.mp4,contact_sheet.png}`. Full details in `notes/sessions/2026-06-26-cross-game-sma4.md`.
## Self-healing rules  (instructions to future-Claude — run EVERY iteration, in order)
1. RECONCILE FIRST. Run `./venv/bin/python scripts/update_status.py`. It rewrites the STATUS block + `runs/status.json`. If git shows the STATUS block changed, reality moved — read it before writing prose.
2. NEVER hand-edit anything between STATUS:BEGIN and STATUS:END. If a number there looks wrong, fix the producing script/result.json, not this file.
3. WHAT WORKS / VERIFIED: add a line ONLY if a named artifact backs it (a result.json field, a passing test, or a contact sheet you visually confirmed). Cite the path. If the backing artifact is gone or its test went red, DELETE the line.
4. ACTIVE ISSUES: when resolved, MOVE to changelog as one line and DELETE from here. Only currently-open items live here.
5. CHANGELOG CAP: at most 10 entries, newest first. Adding an 11th deletes the oldest. Full history is in git — summarize outcomes, don't copy commit messages.
6. SUPERSEDED NUMBERS: any metric also shown in the STATUS block must NOT appear in prose. If best 1-1 framerule_time changes, the old number must not survive anywhere here.
7. NEXT ACTION: keep exactly ONE concrete next action. Replace it when done.
8. ONE-LINE STATUS: keep the top line matching the STATUS block phase.
9. CONSISTENCY CHECK: before finishing, confirm no prose metric contradicts the STATUS block; resolve any DRIFT warning from update_status.py.
10. SESSION NOTES: after meaningful research, implementation, or experiment results, update `notes/session-log.md` and the relevant `notes/sessions/*.md` file with artifacts, verification, and next options. Add new external papers/docs to `notes/research-bibliography.md`.
