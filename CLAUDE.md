# Mario AI — Working Memory

Read this after `AGENTS.md`. The July 25 audit lives in
`notes/sessions/2026-07-25-project-audit-and-next-steps.md`; its option-integrity
follow-up lives in
`notes/sessions/2026-07-26-sma4-option-boundary-integrity.md`; the current
cursor correction, Tier-2 result, and plan live in
`notes/sessions/2026-07-27-sma4-live-cursor-pointer-tier2.md`.
Architecture lives in `DESIGN.md`; research sources live in
`notes/research-bibliography.md`.

## Current status

The reliable core is exact, resettable-emulator search. SMB1 any% is replay-verified
8/8 (`runs/playthrough_anypct/playthrough_result.json`). The stock manifest is
**30/32 replay-verified at seed 0**: `6-2` is unsolved and the former `6-3` positive
is quarantined after replay failure. Reproduce with:

```bash
./venv/bin/python scripts/verify_stock_solutions.py --expect-verified 30
```

Learned models are currently search guidance, not a replacement controller. A local
1-1 rerun reproduced the reported policy-prior reduction from 7005 to 2770 expanded
nodes (60.5% fewer; 2.53 plain/guided ratio), but its checkpoint and training-data
provenance are not clean-clone reproducible yet. The standalone imitation-learning,
DAgger, and reverse-curriculum observations are local experimental evidence, not a
general impossibility result or a preserved benchmark.

SMA4 is a **segmented option-planning prototype**, not yet a continuous Option-SMDP
agent. Exact boundary/root/action hashes, RAM-write ledgers, effective intervention
tiers, and physical lineage are instrumented. Strict legacy search still fails closed
on physical aliases; opt-in BFS/UCS now preserve distinct representatives under one
`MetaState` and refine a finite option table after search. The formerly fixed cursor
pair can name an abandoned object after the 1-3 exit; pointer `0x03007824` selects the
live object at `0x03003DE0` or `0x03004EF8`. Following it removes every cursor write,
and both cached acquisition orders pass the matched `DOWN`/NOOP World-8 response test
at maximum Tier 2. BFS reports 13,266 mixed frames and UCS 13,194; their reported
ROM-option prefixes are 7,266 and 7,194 before a 6,000-frame symbolic Bowser edge.
Legacy option totals can still include small rolled-back probes. Greedy finds no
goal, so this is neither a continuous completion nor a measured speedup.

<!-- STATUS:BEGIN (generated by scripts/update_status.py — DO NOT EDIT BY HAND) -->
Legacy experiment ledger (artifact-derived; this is not the current roadmap):

| Milestone | Status | Key metric |
|---|---|---|
| V0 beat 1-1 (search) | DONE | beat=True, framerule=52, deaths=0 (20260603-130253-v0_1_1) |
| V1 dataset | DONE | n_samples=8748, levels_beaten=11 |
| V2 BC clears 1-1 | DONE | completion_rate=1.0 (20260603-203756-v2_eval) |
| V3 DAgger | DONE | completion_rate=1.0 (20260603-204027-v2_eval) |

Current best: 1-1: beat (fr=52) net=100%; 1-2: net=100%; 1-3: net=0%; 1-4: net=0%; 2-1: net=0%; 4-1: net=0%; 4-2: net=0%; 4-4: net=0%; 8-1: net=0%; 8-2: net=0%; 8-3: net=0%. Tests: full repository suite (determinism/snapshot gate first): 171 passed / 0 failed / 10 skipped; determinism=green. Bench: 1375.7 fps, snapshot 73.076µs, 336.9 nodes/s. _(generated 2026-07-27T22:58:56Z, 426503f-dirty)_
<!-- STATUS:END -->

## Durable verified facts

- Emulator snapshots and deterministic forward dynamics are the trusted low-level
  substrate; the determinism and snapshot tests are the first verification gate.
- SMB1 beam/coverage/segmented search handles ordinary, pipe, water, and loop-castle
  levels. Canonical manifests are accepted only after independent replay.
- `GameAdapter` supports NES SMB1 plus ROM-gated SML and SMA4 paths. NES and GBA
  emulator dependencies require separate environments because their `pyglet`
  constraints conflict.
- SMA4 1-1 and 1-2, both whistle acquisitions, and real whistle-spend transitions
  have replay-backed local artifacts. This validates the components, not continuous
  composition from a single root.
- The live post-1-2 map route to the real fortress is
  `DOWN,DOWN,LEFT -> (96,96)`. From the declared 1-2 root it retains 1,733 frames
  and ends at exact emulator SHA-256 `9173ec00...f9762`, identically twice.
- Stable-Retro/mGBA savestate bytes are not canonical across load/resnapshot. Raw
  equality is exact continuity; RAM/wrapper/fixed-suffix agreement is only a
  labeled finite restore attestation, never proof of general equivalence.
- SMA4 map coordinates must follow the live cursor-object pointer at `0x03007824`.
  A null or unknown pointer in live-map context is unresolved and fails option
  boundaries closed; legacy fixed bytes are diagnostic provenance, not repair
  targets.
- Physical SMA4 BFS/UCS key frontier state by `(MetaState, physical_record_id)`.
  The corrected Tier-2 diagnostic retains 13 physical records in 11 finite
  blocks, repeats 14 enabled branches twice, and has no conformance failure.
  Post-second-whistle and World-8 record pairs share blocks; post-first-whistle
  records remain cost-distinct under `use_whistle_again`.
- The complete/closed refinement claim is scoped to the encountered records,
  finite option library, tier, depth, ROM, and core. It is not global
  bisimulation and does not yet merge the live frontier.

## Active issues

- Stock SMB1 remains 30/32: solve `6-2`; re-solve `6-3` deterministically.
- Learned-result provenance is incomplete: preserve configs, seeds, split metadata,
  checkpoint/data hashes, and machine-readable comparison reports.
- The whistle route restores two independent power-state roots, merges predecessor
  inventory once, and reholds fortress leaf power once. It does not yet establish
  a continuous one-root acquisition history.
- The World-8/Bowser endpoint remains symbolic, and retained option cost still
  needs clean separation from rolled-back evaluation probes.
- Greedy is intentionally nonexploring; a planner advantage requires matched
  exploratory/effect-cache baselines and a declared vector cost.

## Next action

**Starting from one live World-1 root, obtain and preserve the required power
through controller input, acquire both whistles without a second external restore,
remove the inventory-merge and leaf-rehold writes, and independently replay that
continuous physical lineage with zero direct writes at maximum Tier 2.**

## Operating rules

1. Run `./venv/bin/python scripts/update_status.py` before editing this file.
2. Never hand-edit the generated STATUS block. Fix its producing artifact or script.
3. Run `./venv/bin/python scripts/verify_iteration.py` after code changes; it records
   the full suite, with determinism/snapshot first, in `runs/tests.json`.
4. Put experiment detail and superseded history in dated session notes, not here.
5. Record only artifact-backed facts; qualify local observations and missing
   provenance explicitly.
6. Keep open issues current and keep exactly one concrete action in this file.
