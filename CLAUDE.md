# Mario AI — Working Memory (CLAUDE.md)

> Ground truth lives in `runs/status.json` (regenerated by `scripts/update_status.py`).
> This file is the curated narrative. NEVER hand-edit the STATUS block — run the script.
> Follow the **Self-healing rules** at the bottom every iteration. Architecture: `DESIGN.md`.
> Build/verification plan: approved plan (search-teacher → distill → DAgger → value+speed).

## Current status (one line)
**2-2 + 7-2 SOLVED; 8-4 escape is a DOWN-PIPE CHAIN (disassembly-confirmed), chaining-to-flag in progress.** Root cause of all the "TAS-only / dead-end" 8-4 negatives = a DETECTOR BLIND SPOT: gym-super-mario-bros fast-forwards pipe transitions INSIDE `env.step`, so post-step `$0760/$06DE/$000E` never sees the entry — the correct signal is `info.x_pos`-vs-live-RAM mismatch (or raw `sim.u.step` frames). Same bug recovered 2-2 + 7-2 (hold-RIGHT side-pipe; replay beat=True, flag x=3161). For 8-4, Codex + my own SMBDIS.ASM audit confirm: pipe-1 at x1296 is an ENTERABLE vertical pipe (`HandlePipeEntry`: DOWN + foot metatiles `$11/$10` → `$06DE=$30,$000E=3`); `ckpt[:75]` enters it → page-7 lower path (dest aptr `$e5/$07`). The `ProcLoopCommand` gates at pages `$06/$0b/$10` pass only at `$00CE`=240 grounded — the down-pipes drop Mario onto that lower path. (My mid-session "x1296 not enterable" probe was the blind spot again — a false negative, overturned.) From confirmed pipe-1, Go-Explore reaches page 10, stalls at the page-11 gate (needs pipe-2). REMAINING: a reliable down-pipe-entry macro to chain pipes 2+3 → water → side-pipe → Bowser → axe. 13 levels solved (2-2,7-2 new); any% 7/8.

<!-- STATUS:BEGIN (generated by scripts/update_status.py — DO NOT EDIT BY HAND) -->
| Milestone | Status | Key metric |
|---|---|---|
| V0 beat 1-1 (search) | DONE | beat=True, framerule=52, deaths=0 (20260603-130253-v0_1_1) |
| V1 dataset | DONE | n_samples=5091, levels_beaten=3 |
| V2 BC clears 1-1 | DONE | completion_rate=None (20260603-230433-v2_eval) |
| V3 DAgger | DONE | completion_rate=1.0 (20260603-204027-v2_eval) |
| V4 multi-world | TODO | — |
| V5 value+speed | TODO | — |

Current best: 1-1: beat (fr=52) net=0%; 1-2: net=100%; 1-3: net=0%. Tests: 38 passed / 0 failed. Bench: 1375.7 fps, snapshot 73.076µs, 336.9 nodes/s. _(generated 2026-06-06T08:06:52Z, 257511a-dirty)_
<!-- STATUS:END -->

## What works / verified  (each line MUST cite a backing artifact)
- Native arm64 env: torch 2.12 MPS available; gym-super-mario-bros 8.0 / nes-py 9.0 — smoke test passed.
- Determinism + snapshot exactness — `tests/test_determinism.py`, `tests/test_snapshot.py` GREEN (golden committed).
- Death-aware reward invariants — `tests/test_reward.py` GREEN (death dominant, fake-progress counters ignored).
- Artifact contract — `tests/test_artifacts_schema.py` GREEN; `schemas/result.schema.json` locked.
- Emulator throughput measured — `bench/step_rate.json`, `bench/snapshot_cost.json`.
- V0: beam search beats 1-1 — `runs/20260603-130253-v0_1_1/result.json` (pass=True); flagpole visible in its `contact_sheet.png`.
- Tile observation extractor — visually verified (`scripts/verify_observation.py`: ground/pit/stairs align); `tests/test_observation.py` GREEN.
- Self-healing tracking spine works — `update_status.py` regenerated the STATUS block from `runs/status.json`; `verify_iteration.py` runs the loop.
- `label_state` teacher primitive validated — soft targets sharp at hazards (fatal→0, jumps dominate), uniform on open ground; `tests/test_label.py` GREEN. Drove the design: HARD label primary (decisive search action), SOFT auxiliary (entropy-weighted in the loss).
- V1 dataset (1-1) passes its gate — `data/manifest.json` (on-path + perturb-and-recover coverage); `scripts/verify_dataset.py` PASS. K-stacking trajectory-aware (`tests/test_buffer.py` GREEN).
- V2 BC pipeline runs — trained `MarioPolicy` (1.85M params); CPU-vs-MPS parity GREEN (`bench/mps_parity.json`: argmax agreement 1.0, logit diff 2e-6); eval harness + `eval.json` contract (`tests/test_eval_schema.py` GREEN).
- **A learned net beats 1-1 (30/30)** — `runs/20260603-204027-v2_eval/eval.json` (V3, completion 1.0); flagpole visible in its `contact_sheet.png`. Distilled from search + warm-start DAgger.
- Observation timing sensors (gap_ahead pit-distance + x_subtile) were the unlock for the first pit — `tests/test_observation.py::test_gap_ahead_logic` GREEN.
- Multi-stage runner (`mario/runner.py`) + `search_from_state` play continuously, net-primary + live search-rescue; per-level specialists (1-1, 1-2) exist but DON'T transfer to the multi-stage level-start (specialists are single-stage), so the run uses net on 1-1 + search elsewhere.
- Continuous playthrough delivered — `runs/20260604-013306-playthrough_w1plus/full_run.mp4`: 1-1 (net) + 1-2,1-3,1-4,2-1 (search), every level reaches the flag. `solve_and_stitch.py` solves levels offline (no lives limit) + stitches; `scripts/probe_level.py` renders any level's search attempt.
- `coverage_search` (`mario/search.py`) — general solver = beam + Go-Explore coverage (dedup on the cell `(area,x-tile,y-tile)` + per-lineage novelty bonus, area-FIRST progress). Cracked **4-2's vertical/warp-zone maze** where plain beam's beam emptied (x3011); `data/solutions/4-2.json` solved=True. Optional `area_bonus`/`ground_bonus`/`loop_back_px` knobs for maze levels.
- **any% solutions cached (7/8)** — `data/solutions/{1-1,1-2,4-1,4-2,8-1,8-2,8-3}.json` all solved=True (flag reached); stitched to `runs/playthrough_anypct/full_run.mp4` via `scripts/stitch_solutions.py` (1-1 by the learned net, rest by beam/coverage). 10 distinct levels solved total (+1-3,1-4,2-1).
- 8-4 page16→12 loop diagnosed from RAM (`mario/ram.py` addrs): at x≈3847 page=16 Y=108 float=1 (airborne) area=3, pushing right teleports page 16→12 (the SMB ProcLoopCommand back-4-pages loop). This loop is a **DECOY**, not the route — the search stalled there only because it walked PAST the three castle DOWN-pipes (x1296/2432/3648) without grounding+pressing DOWN on the pipe-top. (Superseded the earlier "NO area change reachable" claim, which was a walk-past-the-pipe + detector artifact.)
- **Global progress coordinate Φ (the Jun-4 audit's #1 root-cause fix) — DONE + verified.** `beam_search` now scores level-GLOBAL progress Φ=`area_seq*10000 + (x - area_entry_x)` (via `state_score(progress=)`) with an area-aware `_dedup_key`, so a pipe/area transition reads as forward progress instead of a −1000px collapse. Regression-clean: 1-1 solved (len134) and the multi-area 1-2 solved (len138). Root cause was `x_pos` being area-local (`smb_env.py:149`).
- **$0750 area-pointer fix + room-graph tooling (Codex-confirmed).** Sub-area/pipe transitions flip `$0750` (AreaPointer), NOT `$0760` (verified 1-2/4-2). `area_search` (`mario/search.py`) keys success+cell on `$0750` with `forbid_keys`/`max_x`/`actions`; `solve_area_chain.py` is a room-graph DFS. CAVEAT (Codex): a bare `$0750` flip is a pending pipe-DESTINATION marker (set by row-`$0e` objects), NOT a transition — a REAL entry needs `ChangeAreaTimer $06DE` nonzero / `GameEngineSubroutine $000E`→pipe mode. 8-4 decoded route: markers `($e5,$07)→($65,$0c)→($02,$00 water)→($65,$10 Bowser)`, reached via 3 castle DOWN-pipes (x1296→pg7, x2432→pg12, x3648→water $02) entered by a DOWN-held descent onto the pipe-top (10/11 row6), then a water SIDE-pipe → Bowser → axe. (Superseded the earlier "lethal lava mount → TAS-only" claim — leg 3 is a normal down-pipe, confirmed empirically.) `recon_8_4.py`/`pipe_sweep_8_4.py`/`verify_route_8_4.py` tooling.
- **Value-guided best-first search (ExIt §10) — DONE.** Independent `ValueNet` (`mario/value.py`, val-AUC 0.921) trained from solved trajectories (`gen_value_data.py`/`train_value.py`); `beam_search(value_guide=,value_weight=)` adds `w·V(s)`. Functional (~10% overhead, never breaks solving) but NO node savings on our levels — Φ already solves them at width 5-6; value's benefit is reserved for deceptive levels where plain fails. (Value head kept INDEPENDENT because a shared-trunk BCE head regressed 1-1.)
- **2-2 AND 7-2 (underwater) SOLVED from scratch** — `data/solutions/{2-2,7-2}.json` solved=True; `render.replay` beat=True (both flag x_pos=3161); contact sheets show swim→side-pipe→surface area-2→flagpole. Entry = hold RIGHT into the side-pipe (descend to x3011/Y112 foot-landing frame → side-probe `(X+13)=3024`→`$6c`); 7-2 entry caught as `x_jump` by `real_transition`. KEY GOTCHA: gym fast-forwards pipe transitions inside `env.step()`, so detect entries by the post-step backward x-jump (`mario/ram.py::real_transition`), not `pipe_entering` after the wrapped step. `scripts/solve_22.py`/`solve_72.py`.
- **8-4 / castles 4-4 / 7-4 are GENUINE dead-ends (corrected-detector-confirmed)** — re-ran with `real_transition` (`area_search` `x_jump_px`, `coverage_search` `loop_needs_visited`): 8-4 coverage→x2571 + DFS no transition past the (3,229) dead-end; 4-4/7-4 no transition out of the start room. The maze loop returns to VISITED cells (pruned) with no hidden new-area pipe — so these are real (lava/loop), distinct from the water-level detection bug. `scripts/recon_84_detector.py`, `solve_84.py`, `probe_84_waterpipe.py`.
- **Corrected transition semantics — `mario/ram.py` + `area_search` (Codex disassembly-verified, tests GREEN).** `pipe_entering(ram)` = real pipe/area entry underway (`$06DE!=0` OR `$000E∈{2,3}`); `stage_key(ram)` = `($075f,$075c)` (water-level completion increments `$075c`, no flagpole); `$0750` documented as a PENDING-destination marker (not entry). `area_search` now succeeds on `transitioned()` = flag | pipe_entering | stage-change | new-room. Decoded 2-2's exit to the exact engine probe (`scripts/solve_22_entry.py` drives `(X+13,Y+24)`→pipe metatile). Snapshot/restore fidelity independently re-verified exact.

## Active issues  (open only — move to changelog when resolved)
- [ ] **8-4 (any% blocker) — SOLVABLE via a down-pipe chain; finish chaining to the flag (disassembly-confirmed, `8-4_FINDINGS.md`).** The escape is a chain of ENTERABLE vertical down-pipes, NOT a route-data wall. Disasm ground truth (SMBDIS.ASM, Codex + my own audit): `HandlePipeEntry`(:12270) fires on DOWN held + right-foot metatile `$11` + left-foot `$10` → sets `$06DE=$30`,`$000E=3`; `VerticalPipeData`(:3826) `$11/$10`="leads somewhere" vs `$13/$12`=decoration; 8-4 pipe-1 = obj `$16,$fe` at L_CastleArea6(:4863) = page5 col1 row6. The loop gates (`ProcLoopCommand`:7826, table:7793) at pages `$06/$0b/$10` pass ONLY if `Player_Y_Position $00CE==$f0`(240) AND `Player_State $001D==0`(grounded) at col0 — the down-pipes drop Mario to the Y≈240 LOWER path to satisfy them. **Pipe-1 CONFIRMED**: `ckpt[:75]` enters at x1296→page7 (dest aptr `$e5/$07`, grounded), skipping page-6's gate. From there Go-Explore reaches page 10 then stalls at the page-11 gate (lower-path floor is Y~176, needs pipe-2 to drop to Y240). DETECTOR (the key fix): the gym wrapper fast-forwards the transition INSIDE `env.step`, so post-step `$0760/$06DE/$000E` is BLIND — my mid-session "x1296 not enterable / zero entries" probe was that blind spot (a FALSE NEGATIVE, overturned). Use `info.x_pos`-vs-live-RAM mismatch (or raw `sim.u.step` frames). REMAINING: a reliable down-pipe-entry macro (precise landing-frame DOWN on `$11/$10`, success=info/live mismatch) to chain pipes 2+3 → water area → water side-pipe (the 2-2 recipe) → Bowser → axe; then verify replay-from-reset beat=True + cache + stitch 8/8.
- [ ] Warpless maze castles 4-4/7-4 — re-test with the same down-pipe-entry macro + correct (info/live-mismatch) detector; their loops use the SAME `ProcLoopCommand` Y-position+grounded gates (4-4 table idx pages `$05/$09`; 7-4 is the 6-checkpoint multi-loop). Likely the same blind-spot/walk-past-pipe issue as 8-4, not a true dead-end. Lower priority than finishing 8-4.
- [ ] **Warpless castles 4-4/7-4** — re-test with the corrected detector + the down-pipe descent affordance once built (their "no transition" may be the same walk-past-the-pipe issue as 8-4 legs 2/3). Lower priority than finishing 8-4.
- [ ] Specialists don't transfer single-stage→multi-stage (enemy frame-parity/start differ); only 1-1 (identical first level) transfers.

## Next action  (exactly ONE, concrete)
Build a reliable **down-pipe-entry macro** for 8-4 and chain it to the flag. Pipe-1 is CONFIRMED enterable (`ckpt[:75]`→page7, disasm-verified); the blocker is reliably hitting the precise landing-frame DOWN on the enterable `$11/$10` pipe-tops for pipes 2+3 (detect success via `info.x_pos`-vs-live-RAM mismatch, NOT post-step RAM). Macro: from each pipe approach, sweep descent timing (run-right→fall→hold DOWN) until the mismatch fires + page advances; chain pipe-1→2→3 → water area → water side-pipe (reuse `solve_22.py`) → Bowser area beam → axe. Then replay-from-reset beat=True, cache `data/solutions/8-4.json`, stitch 8/8. (Reuse confirmed: `/tmp/8_4_ckpt.json[:75]`, `scripts/explore_84_lower.py`.)

## Notes / gotchas  (durable facts, not status)
- Use the native arm64 venv (`uv venv --python 3.13 venv`). The `/usr/local` python3.13 is x86_64/Rosetta and disables torch MPS — never use it.
- Run scripts with `PYTORCH_ENABLE_MPS_FALLBACK=1`; keep `torch.compile` / `detect_anomaly` OFF.
- nes-py ≈ 1380 fps/core (verified). Snapshots are NOT picklable → one beam search is in-process; parallelize ACROSS searches (levels/states), not within.
- Reward: `time` weight stays ~0 mid-level (21-frame framerules); raise only for the speed phase / 8-4.
- Determinism tests are torch-free (CPU/RAM) so MPS nondeterminism can't make them flaky.

## Recent changelog  (HARD CAP: last 10 entries, newest first)
- 2026-06-06: **8-4 escape DECODED + pipe-1 confirmed (disassembly, Codex consult + my own SMBDIS.ASM audit).** The recurring 8-4 negatives were a DETECTOR BLIND SPOT: gym fast-forwards pipe transitions inside `env.step`, so post-step `$0760/$06DE/$000E` is blind to down-pipe entries (my own mid-session "x1296 not enterable / zero entries" probe was exactly this false negative — caught + overturned). Correct signal = `info.x_pos`-vs-live-RAM mismatch or raw `sim.u.step` frames. Verified against disasm: `HandlePipeEntry`(:12270) = DOWN + foot metatiles `$11/$10`→`$06DE=$30,$000E=3`; pipe-1 obj `$16,$fe`@L_CastleArea6(:4863); `ProcLoopCommand`(:7826) gates pages `$06/$0b/$10` pass only at `$00CE`=240 grounded (the down-pipes supply that lower path). **Pipe-1 CONFIRMED**: `ckpt[:75]`→page7 (dest `$e5/$07`); Go-Explore from there reaches page 10, stalls at page-11 gate. Saved memory note on the blind spot. REMAINING: reliable down-pipe-entry macro to chain pipes 2+3 → water → side-pipe → Bowser → axe. 43 tests green.
- 2026-06-05: **Full hard-level sweep with the corrected `real_transition` detector.** Added `real_transition` (ram.py) + `x_jump_px` (area_search) + `loop_needs_visited` (coverage_search), additive, tests green (41). **7-2 SOLVED** (same 2-2 recipe; entry caught as x_jump; replay beat=True x3161, contact sheet). **8-4 / 4-4 / 7-4 CONFIRMED genuine dead-ends** (not detection bugs): 8-4 coverage→x2571, DFS no transition past the (3,229) dead-end; castles no transition out of the start room. So the detection bug afflicted only the WATER side-pipes (destination shares area bytes); the maze loops return to visited cells with no hidden pipe. 12 levels solved; any% still 7/8.
- 2026-06-05: **2-2 SOLVED from scratch** (Codex gpt-5.5 consult was decisive). Root cause of all prior "TAS-only" negatives = a DETECTION BUG: `gym_super_mario_bros` runs `_skip_change_area`/`_skip_occupied_states` INSIDE `env.step()`, fast-forwarding the pipe transition before live RAM is read — so `pipe_entering` never saw `$000E=2`/`$06DE`, and the real entries looked like "ejections to x≈2872". The entry is reachable by just holding RIGHT (descend → x3011/Y112 foot-landing frame → side-probe `(X+13)=3024` hits `$6c`). Solve = `path[:220]`(cf8)+81×right(cf1)+17-action area-2 beam to flagpole; normalized cf1, cached, `render.replay` beat=True (flag x=3161), contact sheet confirms, 38/38 tests green. Reopens 7-2/8-4/castles (same blind detector). `2-2_FINDINGS.md` rewritten.
- 2026-06-05: Reverse-curriculum solver built + run on 2-2 (`scripts/solve_22_land.py`) — rigorous NEGATIVE (the user-chosen from-scratch lever). Codex pinned the exact side-pipe predicate (probe `(X+13,row(Y+24))==0x6c` + `$001D==0`, but `$0704` forces `$001D=1` in water so it only holds on a foot-LANDING frame). Construct-via-poke can't make the goal (SwimmingFlag overwrites `$001D`); 139,725 landing rollouts from 575 near-mouth states → 0; reachability beam proves 0 states with `x≥3011 ∧ Y∈[104,119]` (trigger cell behind the solid `col3024` wall). ⇒ 2-2 = sub-pixel TAS / route-data, same as 8-4. ALL remaining unbeaten levels now proven route-data-class.
- 2026-06-05: 2-2 (underwater) fully decoded + transition semantics CORRECTED (`2-2_FINDINGS.md`, Codex disassembly dive). `$0750`=pending-dest marker (not entry); real entry = `$06DE!=0`/`$000E∈{2,3}`; water-level completion = `$075c` stage byte (no flagpole). Added `pipe_entering`/`stage_key` to `ram.py`; `area_search` now succeeds on `transitioned()` (flag|pipe_entering|stage|new-room) — tests green (38). 2-2 exit = horizontal side-pipe at level-x 3024 row6 `0x6c`; entry is FRAME-PRECISE (engine probe `(X+13,Y+24)`→`0x6c` + `$001D==0`; row-6 mouth sandwiched by solid row-5 lip + row-7 floor). Search drives the exact probe onto `0x6b` but never `0x6c` → 8-4-class wall. Snapshot fidelity independently re-verified perfect.
- 2026-06-04: Value-guided best-first search built (independent ValueNet, AUC 0.921; `value.py`/`gen_value_data.py`/`train_value.py`; `beam_search(value_guide=)`) — functional but no node-savings on Φ-solvable levels (honest). Codex 2nd+3rd dives: `$0750`=pending-pipe-DESTINATION-marker (not a transition); real entry = `$06DE` nonzero. 8-4 route decoded (water=($02,$00) pipe at page14 x3648-3679, across lava → lethal mount → TAS-only). CONSENSUS w/ Codex: 8-4 = route-data benchmark; pivot to foundation + apply room-graph DFS to warpless. Distillation audit tweaks (drop class-balance, MSE→BCE) TESTED → regressed 1-1 → reverted.
- 2026-06-04: Jun-4 research swarm (8 PhD subagents) → corrected the 8-4 diagnosis: the blocker was a REWARD-COORDINATE bug (x_pos is area-local), not pure exploration. Implemented + verified the global progress coordinate Φ in beam_search (1-1 & multi-area 1-2 both still solve). Added `area_search`. 8-4 area-3 is a pipe/Cheep-Cheep/lava loop needing verified route data.
- 2026-06-04: `coverage_search` (beam+Go-Explore: cell dedup `(area,x,y)` + novelty, area-first) added — cracked 4-2's vertical maze. Solved any% 4-1/4-2/8-1/8-2/8-3 in parallel → 7/8 any% stitched (`runs/playthrough_anypct/full_run.mp4`); 10 levels total. 8-4 page-16 loop diagnosed from RAM; resists all search variants (the lone any% blocker).
- 2026-06-04: Multi-stage runner + search_from_state + solve_and_stitch; continuous playthrough video (1-1 net + 1-2/1-3/1-4/2-1 search). Frontier: underwater + maze-loop castles block the full beat.
- 2026-06-03: V4a — migrated to 9-action set (+down/+up, for pipes/warps/crouch) + 5-channel/1048-dim obs (HAZARD channel + is_water); 1-1 re-verified 100%. Fixed DAgger bug: act_with_entropy must apply the same action_bias as act (else it collects a different policy).
## Self-healing rules  (instructions to future-Claude — run EVERY iteration, in order)
1. RECONCILE FIRST. Run `./venv/bin/python scripts/update_status.py`. It rewrites the STATUS block + `runs/status.json`. If git shows the STATUS block changed, reality moved — read it before writing prose.
2. NEVER hand-edit anything between STATUS:BEGIN and STATUS:END. If a number there looks wrong, fix the producing script/result.json, not this file.
3. WHAT WORKS / VERIFIED: add a line ONLY if a named artifact backs it (a result.json field, a passing test, or a contact sheet you visually confirmed). Cite the path. If the backing artifact is gone or its test went red, DELETE the line.
4. ACTIVE ISSUES: when resolved, MOVE to changelog as one line and DELETE from here. Only currently-open items live here.
5. CHANGELOG CAP: at most 10 entries, newest first. Adding an 11th deletes the oldest. Full history is in git — summarize outcomes, don't copy commit messages.
6. SUPERSEDED NUMBERS: any metric also shown in the STATUS block must NOT appear in prose. If best 1-1 framerule_time changes, the old number must not survive anywhere here.
7. NEXT ACTION: keep exactly ONE concrete next action. Replace it when done.
8. ONE-LINE STATUS: keep the top line matching the STATUS block phase.
9. CONSISTENCY CHECK: before finishing, confirm no prose metric contradicts the STATUS block; resolve any DRIFT warning from update_status.py.
